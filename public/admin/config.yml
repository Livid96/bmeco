backend:
  name: github
  repo: your-username/your-repo-name # Replace with your repo
  branch: main # or master
  open_authoring: true

# ADD THIS LINE BELOW BACKEND
publish_mode: editorial_workflow

# The rest of your config (media_folder, collections, etc.)
media_folder: "public/images"
public_folder: "/images"
